# SERVER ONLY. Real values go in the hosting secret manager, NEVER in GitHub or browser files.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
# Generate locally: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SESSION_ENCRYPTION_KEY=
APP_ORIGIN=
# Archil's chosen owner email. Must be verified; no value has been assumed.
OWNER_EMAIL=
# Local development ONLY. Production must omit or use 0.
IC_DEVELOPMENT=0
